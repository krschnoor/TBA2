import { TBClassifiedPipe } from './tbclassified.pipe';

describe('TBClassifiedPipe', () => {
  it('create an instance', () => {
    const pipe = new TBClassifiedPipe();
    expect(pipe).toBeTruthy();
  });
});
